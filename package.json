{
  "name": "re-turbo",
  "scripts": {
    "build": "rescript build -with-deps",
    "clean": "rescript clean && rimraf dist/",
    "refmt": "rescript format src/*",
    "dev": "vite",
    "vite:build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write \"src/**/*.ts\""
  },
  "dependencies": {
    "reprocessing": "github:RawToast/reprocessing#ml"
  },
  "devDependencies": {
    "rescript": "^10.1.3",
    "rimraf": "^3.0.0",
    "vite": "^5.1.6"
  }
}
